# Lab Report Ingestion & MDR Alert System - Architecture

## Complete System Workflow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        LAB REPORT UPLOAD                                 │
│                 POST /api/lab-reports/upload                             │
│                                                                           │
│  {                                                                        │
│    "patientId": 123,                                                     │
│    "reportId": "LRP-8891",                                               │
│    "organism": "ESBL",                                                   │
│    "sampleType": "Urine",                                                │
│    ...                                                                    │
│  }                                                                        │
└────────────────────────┬──────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    LAB REPORT SERVICE                                    │
│                 labReportService.js                                      │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────┐           │
│  │ 1. VALIDATE PATIENT EXISTS                                │           │
│  │    └─> SELECT * FROM patients WHERE patient_id = ?       │           │
│  └──────────────────────────────────────────────────────────┘           │
│                         │                                                 │
│                         ▼                                                 │
│  ┌──────────────────────────────────────────────────────────┐           │
│  │ 2. SAVE LAB REPORT                                        │           │
│  │    └─> INSERT INTO lab_reports (...)                     │           │
│  └──────────────────────────────────────────────────────────┘           │
│                         │                                                 │
│                         ▼                                                 │
│  ┌──────────────────────────────────────────────────────────┐           │
│  │ 3. CHECK IF ORGANISM IS MDR                               │           │
│  │    └─> mdrDetectionService.checkMDROrganism()            │           │
│  │        SELECT * FROM mdr_list WHERE organism = 'ESBL'    │           │
│  └──────────────────────────────────────────────────────────┘           │
│                         │                                                 │
│                    ┌────┴────┐                                           │
│                    │         │                                           │
│            isMDR=true   isMDR=false                                      │
│                    │         │                                           │
└────────────────────┼─────────┼───────────────────────────────────────────┘
                     │         │
                     │         └──> End workflow (report saved only)
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     FLAG PATIENT AS MDR+                                 │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────┐           │
│  │ 4. CREATE MDR FLAG                                        │           │
│  │    INSERT INTO mdr_flags (                                │           │
│  │      patient_id, organism, report_id,                     │           │
│  │      severity, status='active'                            │           │
│  │    )                                                       │           │
│  │    └─> Prevents duplicates with UNIQUE constraint        │           │
│  └──────────────────────────────────────────────────────────┘           │
│                         │                                                 │
│                         ▼                                                 │
│  ┌──────────────────────────────────────────────────────────┐           │
│  │ 5. CREATE NOTIFICATION RECORD                             │           │
│  │    INSERT INTO notifications (                            │           │
│  │      patient_id, flag_id,                                 │           │
│  │      message="ALERT: Patient tested positive for MDR-ESBL"│           │
│  │      channel='all', sent=FALSE                            │           │
│  │    )                                                       │           │
│  └──────────────────────────────────────────────────────────┘           │
│                         │                                                 │
│                         ▼                                                 │
│  ┌──────────────────────────────────────────────────────────┐           │
│  │ 6. PUBLISH EVENT TO MESSAGE QUEUE                         │           │
│  │    eventPublisher.publishMDRAlert({                       │           │
│  │      patientId, organism, severity, flagId                │           │
│  │    })                                                      │           │
│  └──────────────────────────────────────────────────────────┘           │
└────────────────────────┬──────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                       REDIS MESSAGE QUEUE                                │
│                                                                           │
│  Channel: "mdr:alerts"                                                   │
│  Queue:   "mdr:alert:queue"                                              │
│                                                                           │
│  Event: {                                                                │
│    type: "mdr.alert.triggered",                                          │
│    data: {                                                               │
│      patientId: 123,                                                     │
│      patientName: "John Doe",                                            │
│      organism: "ESBL",                                                   │
│      severity: "high",                                                   │
│      flagId: 42                                                          │
│    }                                                                      │
│  }                                                                        │
└────────────────────────┬──────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    NOTIFICATION WORKER                                   │
│              notificationWorker.js (Background Process)                  │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────┐           │
│  │ 1. LISTEN TO REDIS QUEUE                                 │           │
│  │    redis.subscribe('mdr:alerts')                          │           │
│  └──────────────────────────────────────────────────────────┘           │
│                         │                                                 │
│                         ▼                                                 │
│  ┌──────────────────────────────────────────────────────────┐           │
│  │ 2. FETCH NOTIFICATION FROM DB                             │           │
│  │    SELECT * FROM notifications                            │           │
│  │    WHERE patient_id = ? AND sent = FALSE                  │           │
│  └──────────────────────────────────────────────────────────┘           │
│                         │                                                 │
│                         ▼                                                 │
│  ┌──────────────────────────────────────────────────────────┐           │
│  │ 3. GET RECIPIENTS                                         │           │
│  │    - Query users by role (doctor, nurse, infection_ctrl) │           │
│  │    - Get FCM tokens, phone numbers, emails               │           │
│  └──────────────────────────────────────────────────────────┘           │
│                         │                                                 │
│                         ▼                                                 │
│  ┌──────────────────────────────────────────────────────────┐           │
│  │ 4. SEND MULTI-CHANNEL NOTIFICATIONS                       │           │
│  │    notificationService.sendMultiChannel()                 │           │
│  └──────────────────────────────────────────────────────────┘           │
└────────────────────────┬──────────────────────────────────────────────────┘
                         │
                    ┌────┴────┬─────────┬─────────┐
                    │         │         │         │
                    ▼         ▼         ▼         ▼
┌──────────────┬──────────┬─────────┬──────────┬────────────────────────┐
│              │          │         │          │                        │
│   PUSH       │ WEBSOCKET│  SMS    │  EMAIL   │   AUDIT LOG            │
│   (FCM)      │  (WSS)   │(Twilio) │(SMTP)    │                        │
│              │          │         │          │                        │
│  ┌────────┐  │ ┌──────┐ │┌──────┐ │┌───────┐ │  INSERT INTO           │
│  │Firebase│  │ │All   │ ││Twilio│ ││SMTP  │ │  mdr_audit_log         │
│  │Admin   │  │ │Active│ ││API   │ ││Server│ │  (action, patient_id,  │
│  │SDK     │  │ │Clients│││      │ ││      │ │   details)             │
│  └────────┘  │ └──────┘ │└──────┘ │└───────┘ │                        │
│              │          │         │          │                        │
│  • Tokens    │ • Real-  │• Phone  │• Email   │  • Track all actions   │
│  • Title     │   time   │  number │  address │  • Who did what        │
│  • Body      │ • Live   │• Message│• Subject │  • When                │
│  • Data      │   dashboard        │• HTML   │                        │
│              │  updates │         │  body    │                        │
│              │          │         │          │                        │
└──────────────┴──────────┴─────────┴──────────┴────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                   UPDATE NOTIFICATION STATUS                             │
│                                                                           │
│  UPDATE notifications                                                    │
│  SET sent = TRUE, sent_at = NOW()                                       │
│  WHERE notif_id = ?                                                      │
└─────────────────────────────────────────────────────────────────────────┘

```

## Database Schema Overview

```
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│  patients    │◄─────│ lab_reports  │─────►│  mdr_list    │
│              │      │              │      │              │
│ • patient_id │      │ • report_id  │      │ • organism   │
│ • mrn        │      │ • patient_id │      │ • severity   │
│ • name       │      │ • organism   │      │ • isolation  │
│ • dob        │      │ • test_name  │      └──────────────┘
└──────┬───────┘      │ • sample_type│
       │              │ • result_at  │
       │              └──────┬───────┘
       │                     │
       │                     │
       ▼                     ▼
┌──────────────┐      ┌──────────────┐
│  mdr_flags   │      │notifications │
│              │      │              │
│ • flag_id    │◄─────┤ • notif_id   │
│ • patient_id │      │ • patient_id │
│ • organism   │      │ • flag_id    │
│ • severity   │      │ • message    │
│ • status     │      │ • channel    │
│ • flagged_at │      │ • sent       │
└──────────────┘      └──────────────┘
```

## API Endpoints Flow

```
┌─────────────────────────────────────────────────────────┐
│          CLIENT (Frontend/Postman/cURL)                  │
└────────────┬────────────────────────────────────────────┘
             │
             │  POST /api/lab-reports/upload
             │  Authorization: Bearer <jwt-token>
             │  Body: { patientId, reportId, organism, ... }
             │
             ▼
┌─────────────────────────────────────────────────────────┐
│               EXPRESS SERVER (server.js)                 │
│                                                           │
│  ┌────────────────────────────────────────────┐         │
│  │ Middleware Stack:                          │         │
│  │  1. CORS                                   │         │
│  │  2. Body Parser                            │         │
│  │  3. JWT Auth (requireAuth)                │         │
│  │  4. Validation (express-validator)        │         │
│  └────────────────────────────────────────────┘         │
└────────────┬────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────┐
│         ROUTE HANDLER (lab-reports.js)                   │
│                                                           │
│  • Validate request body                                 │
│  • Extract data                                          │
│  • Call service layer                                    │
└────────────┬────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────┐
│       SERVICE LAYER (labReportService.js)                │
│                                                           │
│  • Business logic                                        │
│  • Transaction management                                │
│  • Orchestrate workflow                                  │
└────────────┬────────────────────────────────────────────┘
             │
    ┌────────┴────────┬────────────┬──────────────┐
    │                 │            │              │
    ▼                 ▼            ▼              ▼
┌─────────┐  ┌──────────────┐  ┌────────┐  ┌───────────┐
│Database │  │MDR Detection │  │Event   │  │Audit      │
│Queries  │  │Service       │  │Publisher│  │Service    │
└─────────┘  └──────────────┘  └────────┘  └───────────┘
```

## Real-Time Notification Flow

```
┌─────────────────────────────────────────────────────────┐
│              NOTIFICATION CHANNELS                       │
└─────────────────────────────────────────────────────────┘

1. WEBSOCKET (Real-time dashboard)
   ════════════════════════════════
   
   Client:
   ┌──────────────────────────────┐
   │ const ws = new WebSocket(    │
   │   'ws://localhost:5000/      │
   │    api/notifications/live    │
   │    ?userId=123'              │
   │ );                           │
   │                              │
   │ ws.onmessage = (event) => {  │
   │   const data = JSON.parse    │
   │     (event.data);            │
   │   showAlert(data);           │
   │ };                           │
   └──────────────────────────────┘
   
   Server broadcasts:
   {
     "type": "mdr_alert",
     "data": {
       "patientId": 123,
       "message": "ALERT: MDR detected",
       "priority": "high"
     }
   }

2. PUSH NOTIFICATIONS (Mobile/Desktop)
   ═══════════════════════════════════
   
   Firebase Cloud Messaging:
   ┌──────────────────────────────┐
   │ admin.messaging()            │
   │   .sendMulticast({           │
   │     notification: {          │
   │       title: "🚨 MDR Alert", │
   │       body: "Patient #123..."│
   │     },                       │
   │     tokens: [fcmToken1, ...]  │
   │   })                         │
   └──────────────────────────────┘

3. SMS (Critical alerts)
   ═════════════════════
   
   Twilio Integration:
   ┌──────────────────────────────┐
   │ twilio.messages.create({     │
   │   body: "URGENT: MDR Alert", │
   │   from: "+1234567890",       │
   │   to: "+0987654321"          │
   │ })                           │
   └──────────────────────────────┘

4. EMAIL (Detailed reports)
   ════════════════════════
   
   SMTP/Nodemailer:
   ┌──────────────────────────────┐
   │ transporter.sendMail({       │
   │   from: "alerts@hospital",   │
   │   to: "doctor@hospital.com", │
   │   subject: "MDR Alert",      │
   │   html: "<full report>"      │
   │ })                           │
   └──────────────────────────────┘
```

## Complete Technology Stack

```
Frontend Layer
═══════════════
• React.js / Vue.js / Angular
• WebSocket client
• Firebase SDK (push notifications)
• Axios (HTTP requests)

Backend Layer
════════════════
• Node.js + Express.js
• JWT authentication
• express-validator
• WebSocket (ws package)

Database Layer
═════════════════
• MySQL 8.0+
  - Patients
  - Lab Reports
  - MDR List
  - Flags
  - Notifications
  - Audit Logs

Message Queue
════════════════
• Redis (PubSub + Lists)
  - Channels: mdr:alerts
  - Queues: mdr:alert:queue
  - Alternative: RabbitMQ, Kafka

Background Workers
═════════════════════
• Node.js worker processes
• Event consumers
• Notification dispatchers

External Services
════════════════════
• Firebase Cloud Messaging (FCM)
• Twilio (SMS)
• SMTP/SendGrid (Email)
```

---

## Summary

This architecture provides:
✅ **Automated MDR detection** - Instant organism checking
✅ **Real-time alerting** - Multiple notification channels
✅ **Scalable design** - Message queue for async processing
✅ **Audit trail** - Complete history of all actions
✅ **Duplicate prevention** - Database constraints
✅ **Transaction safety** - MySQL transactions
✅ **Multi-channel** - Push, WebSocket, SMS, Email
✅ **Background processing** - Workers handle notifications
✅ **RESTful API** - Clean endpoint design
✅ **Comprehensive testing** - Automated test suite
